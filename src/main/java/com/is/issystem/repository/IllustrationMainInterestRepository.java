package com.is.issystem.repository;

import com.is.issystem.entities.IllustrationMainIntserest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IllustrationMainInterestRepository extends JpaRepository<IllustrationMainIntserest,Integer> {
}
