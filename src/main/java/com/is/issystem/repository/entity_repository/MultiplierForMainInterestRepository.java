package com.is.issystem.repository.entity_repository;

import com.is.issystem.entities.MultiplierForMainInterest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MultiplierForMainInterestRepository extends JpaRepository<MultiplierForMainInterest,Integer> {
}
